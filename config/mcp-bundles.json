{
  "bundles": {
    "core": {
      "servers": [
        {
          "name": "conductor",
          "command": "conductor",
          "args": ["mcp"]
        }
      ]
    },
    "extended": {
      "servers": [
        {
          "name": "conductor",
          "command": "conductor",
          "args": ["mcp"]
        },
        {
          "name": "search",
          "command": "",
          "args": [],
          "enabled": false,
          "note": "Add a search MCP server command here."
        },
        {
          "name": "lsp",
          "command": "",
          "args": [],
          "enabled": false,
          "note": "Add an LSP MCP server command here."
        },
        {
          "name": "ast",
          "command": "",
          "args": [],
          "enabled": false,
          "note": "Add an AST MCP server command here."
        }
      ]
    },
    "gemini-official": {
      "servers": [
        {
          "name": "gemini-cloud-assist",
          "command": "npx",
          "args": [
            "-y",
            "@google-cloud/gemini-cloud-assist-mcp@latest"
          ],
          "note": "Requires gcloud auth application-default login, Node.js 20+, and roles/serviceusage.serviceUsageAdmin + roles/geminicloudassist.user."
        }
      ]
    },
    "gemini-cli": {
      "servers": [
        {
          "name": "gemini-cli",
          "command": "conductor",
          "args": ["mcp-gemini"],
          "note": "Uses Gemini CLI login (gemini -p ...) for MCP bridge."
        }
      ]
    },
    "claude-cli": {
      "servers": [
        {
          "name": "claude-cli",
          "command": "conductor",
          "args": ["mcp-claude"],
          "note": "Uses Claude CLI login (claude -p ...) for MCP bridge."
        }
      ]
    }
  }
}
