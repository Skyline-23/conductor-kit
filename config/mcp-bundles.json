{
  "bundles": {
    "extended": {
      "servers": [
        {
          "name": "search",
          "command": "",
          "args": [],
          "enabled": false,
          "note": "Add a search MCP server command here."
        },
        {
          "name": "lsp",
          "command": "",
          "args": [],
          "enabled": false,
          "note": "Add an LSP MCP server command here."
        },
        {
          "name": "ast",
          "command": "",
          "args": [],
          "enabled": false,
          "note": "Add an AST MCP server command here."
        }
      ]
    },

    "cli-headless": {
      "servers": [
        {
          "name": "codex-cli",
          "command": "codex",
          "args": ["mcp-server"],
          "enabled": false,
          "note": "Codex CLI in MCP server mode (stdio)."
        },
        {
          "name": "claude-code",
          "command": "claude",
          "args": ["mcp", "serve"],
          "enabled": false,
          "note": "Claude Code in MCP server mode (stdio)."
        }
      ]
    },

    "conductor": {
      "servers": [
        {
          "name": "conductor",
          "command": "conductor",
          "args": ["mcp"],
          "note": "Unified MCP server with codex/claude/gemini + role-based conductor routing."
        }
      ]
    }
  }
}
