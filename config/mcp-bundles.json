{
  "bundles": {
    "extended": {
      "servers": [
        {
          "name": "search",
          "command": "",
          "args": [],
          "enabled": false,
          "note": "Add a search MCP server command here."
        },
        {
          "name": "lsp",
          "command": "",
          "args": [],
          "enabled": false,
          "note": "Add an LSP MCP server command here."
        },
        {
          "name": "ast",
          "command": "",
          "args": [],
          "enabled": false,
          "note": "Add an AST MCP server command here."
        }
      ]
    },

    "gemini-cli": {
      "servers": [
        {
          "name": "gemini-cli",
          "command": "conductor",
          "args": ["mcp-gemini"],
          "note": "Uses Gemini CLI login (gemini -p ...) for MCP bridge."
        }
      ]
    },
    "claude-cli": {
      "servers": [
        {
          "name": "claude-cli",
          "command": "conductor",
          "args": ["mcp-claude"],
          "note": "Uses Claude CLI login (claude -p ...) for MCP bridge."
        }
      ]
    },
    "codex-cli": {
      "servers": [
        {
          "name": "codex-cli",
          "command": "conductor",
          "args": ["mcp-codex"],
          "note": "Uses Codex CLI login (codex exec --json) for MCP bridge."
        }
      ]
    }
  }
}
